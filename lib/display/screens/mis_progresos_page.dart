import 'package:flutter/material.dart';

class MisProgresosPage extends StatelessWidget {
  const MisProgresosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mis progresos', textScaler: TextScaler.linear(2)),
    );
  }
}
