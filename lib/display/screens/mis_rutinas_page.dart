import 'package:flutter/material.dart';

class MisRutinasPage extends StatelessWidget {
  const MisRutinasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mis rutinas', textScaler: TextScaler.linear(2)),
    );
  }
}
