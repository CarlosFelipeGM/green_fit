import 'package:flutter/material.dart';

class MisRetosPage extends StatelessWidget {
  const MisRetosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mis retos', textScaler: TextScaler.linear(2)),
    );
  }
}
