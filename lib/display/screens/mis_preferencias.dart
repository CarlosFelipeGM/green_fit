import 'package:flutter/material.dart';

class MisPreferencias extends StatelessWidget {
  const MisPreferencias({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mi configuración', textScaler: TextScaler.linear(2)),
    );
  }
}
